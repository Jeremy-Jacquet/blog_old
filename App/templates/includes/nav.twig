{% import 'macros/nav.twig' as nav %}

<nav class="navbar navbar-expand-lg bg-secondary fixed-top" id="mainNav">
    <div class="container">
        <a class="navbar-brand js-scroll-trigger" href="{{ URL }}accueil">Mon BloG</a>
        <button class="navbar-toggler navbar-toggler-right font-weight-bold bg-primary text-white rounded" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">Menu <i class="fas fa-bars"></i></button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            
            <ul class="navbar-nav ml-auto">
            {% if page == 'admin' %}
                {{ nav.li('Articles', 'admin/articles', {class_a: 'a-admin'}) }}
                {{ nav.li('Commentaires', 'admin/commentaires', {class_a: 'a-admin'}) }}
                {{ nav.li('Auteurs', 'admin/auteurs', {class_a: 'a-admin'}) }}
                {{ nav.li('Utilisateurs', 'admin/utilisateurs', {class_a: 'a-admin'}) }}
                {{ nav.li('Deconnexion', 'deconnexion') }}
            {% else %}
                {{ nav.li('Actu','accueil#actu') }}
                {{ nav.li('Articles', 'articles') }}
                {% if page == 'accueil' %}
                    {{ nav.li('Contact', 'accueil#contact') }}
                {% else %}
                    {{ nav.li('Contact', '', {data_toggle: 'modal', data_target: '#section-contact'}) }}
                {% endif %}
                {{ nav.li('Inscription', '', {data_toggle: 'modal', data_target: '#form-register'}) }}
                {{ nav.li('Connexion', '', {data_toggle: 'modal', data_target: '#form-connexion'}) }}
                {{ nav.li('Admin', 'admin', {class_a: 'a-admin'}) }}
            {% endif %}
            </ul>
            
            </ul>
        </div>
    </div>
</nav>